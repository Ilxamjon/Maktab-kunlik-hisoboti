# Nusxa: /etc/nginx/sites-available/maktabhisobot
# server_name ni o'z domeningizga almashtiring, keyin:
#   sudo ln -s /etc/nginx/sites-available/maktabhisobot /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#   sudo certbot --nginx -d hisobot.example.uz

server {
    listen 80;
    listen [::]:80;
    server_name hisobot.example.uz;

    client_max_body_size 1m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Authorization $http_authorization;
        proxy_read_timeout 60s;
    }
}
